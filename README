#Description

This application uses switches, buttons, and the LED display. The user chooses two numbers from 0-3.
The first value is stored using flip-flips. Once the second value is chosen, the user adds the two values
by pressing button C on the FPGA.
